README — Лабораторна робота №4
Тема

Модульне тестування в Java за допомогою TestNG

Що було реалізовано

У лабораторній роботі було створено систему бронювання номерів у готелі.

Реалізовані класи:

Room
Guest
Booking
Hotel
Основні можливості
створення номерів;
створення гостей;
бронювання номерів;
скасування бронювання;
пошук вільних номерів;
обчислення вартості проживання.
Що було додано у ЛР4

У порівнянні з попередньою лабораторною роботою було додано:

TestNG-тестування

Створено модульні тести для перевірки роботи класів.

Реалізовано:

RoomTest
GuestTest
BookingTest
HotelTest
Maven

Проєкт переведено на Maven.

Додано:

pom.xml
залежності TestNG;
Maven Surefire Plugin;
JaCoCo.
Покриття тестами

Перевірено:

створення об’єктів;
валідацію параметрів;
винятки;
методи класів;
логіку бронювання.
